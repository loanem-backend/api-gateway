SuccessResponse:
  type: object
  properties:
    success:
      type: boolean
    message:
      type: string
  required: [success, message]

IdInt:
  type: object
  properties:
    id:
      type: integer
  required: [id]

TokenResponse:
  type: object
  properties:
    access_token:
      type: string
