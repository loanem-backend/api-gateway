post:
  tags: [auth]
  security: []
  parameters:
    - $ref: ../../components/parameter/refresh_token.yaml#/RefreshTokenCookie
  responses:
    '201':
      description: Successfully logged in.
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schema/general.yaml#/SuccessResponse'
              - type: object
                properties:
                  data:
                    $ref: ../../components/schema/general.yaml#/TokenResponse
